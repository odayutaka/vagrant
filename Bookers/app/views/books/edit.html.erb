<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	<h4>Title</h4>
	<%= f.text_field :title %>
	<h4>Body</h4>
	<%= f.text_field :body %>
	<%= f.submit 'Update Book' %>
<% end %>

<p>
	<%= link_to "Show", show_book_path(@book) %>
	<%= link_to "Back", index_book_path(@book) %>
</p>